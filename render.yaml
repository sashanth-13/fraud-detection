services:
  - type: web
    name: fraudguard-ai
    runtime: python
    rootDir: fraud_detection_system
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
